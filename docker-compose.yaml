# version: '3.8'
#MongoDB 5.0+ requires a CPU with AVX support, => sử dụng image: mongo:4.4
version: '3.8'
services:
  be-nest:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    expose:
      - 1604
    ports:
      - 1604:1604

volumes:
  mongodb:
